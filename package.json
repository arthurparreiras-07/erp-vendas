{
  "name": "erp-vendas",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/api",
    "packages/client"
  ],
  "scripts": {
    "dev": "make dev",
    "build": "make build",
    "typecheck": "make typecheck",
    "prepare": "husky"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "typescript": "^5.4.5"
  },
  "lint-staged": {
    "packages/api/src/**/*.ts": "bash -c 'cd packages/api && npx tsc --noEmit'",
    "packages/client/src/**/*.{ts,tsx}": "bash -c 'cd packages/client && npx tsc --noEmit'"
  }
}
